version: 1
AppDir:
  path: ./AppDir

  app_info:
    id: pybitmessage
    name: PyBitmessage
    icon: pybitmessage
    version: !ENV ${APP_VERSION}
    # Set the python executable as entry point
    exec: usr/bin/python
    # Set the application main script path as argument.
    # Use '$@' to forward CLI parameters
    exec_args: "$APPDIR/usr/bin/pybitmessage $@"

  after_runtime:
    - sed -i "s|GTK_.*||g" AppDir/AppRun.env
    - cp packages/AppImage/qt.conf AppDir/usr/bin/

  apt:
    arch: !ENV '${ARCH}'
    sources:
      - sourceline: 'deb [arch=amd64] https://archive.debian.org/debian buster main'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x648ACFD622F3D138'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x0E98404D386FA1D9'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0xDCC9EFBF77E11517'
      - sourceline: 'deb [arch=amd64] https://archive.debian.org/debian-security buster/updates main'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x112695A0E562B32A'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x54404762BBB6E853'
      - sourceline: 'deb [arch=amd64] https://archive.debian.org/debian buster-updates main'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x648ACFD622F3D138'
        key_url: 'http://keyserver.ubuntu.com/pks/lookup?op=get&search=0x0E98404D386FA1D9'

    include:
      - python-qt4
      - python-setuptools
      - python-sip
      - python-six
    exclude:
      - python-defusedxml
      - python-jsonrpclib
      - python-msgpack
      - python-qrcode
      - python-xdg
      - sni-qt
      - libdb5.3
      - libdbus-1-3
      - libfontconfig1
      - libfreetype6
      - libglib2.0-0
      - libice6
      - libmng2
      - libncursesw5
      - libqt4-declarative
      - libqt4-designer
      - libqt4-help
      - libqt4-script
      - libqt4-scripttools
      - libqt4-sql
      - libqt4-test
      - libqt4-xmlpatterns
      - libqtassistantclient4
      - libsm6
      - libsystemd0
      - libreadline7

  files:
    exclude:
      - usr/lib/x86_64-linux-gnu/gconv
      - usr/share/man
      - usr/share/doc

  runtime:
    arch: [ !ENV '${RUNTIME}' ]
    env:
      # Set python home
      # See https://docs.python.org/3/using/cmdline.html#envvar-PYTHONHOME
      PYTHONHOME: '${APPDIR}/usr'
      # Path to the site-packages dir or other modules dirs
      # See https://docs.python.org/3/using/cmdline.html#envvar-PYTHONPATH
      PYTHONPATH: '${APPDIR}/usr/lib/python2.7/site-packages'

AppImage:
  arch: !ENV '${APPIMAGE_ARCH}'
